/**
 * Asset Registry — Public API
 */

export {
  createAsset,
  createVideoAsset,
  createImageAsset,
  findAsset,
  findAssetsByType,
  getVideoAsset,
  addAsset,
  removeAsset,
  updateAssetSource,
} from "./AssetRegistry";
